import { Button } from '@mui/material';

const Landing = (): JSX.Element => {
    return (
        <div>
            <h1>Welcome to ReadKeeper!</h1>
            <Button>Press away</Button>
        </div>
    );
};

export default Landing